using UnityEngine;

namespace Runtime.Message
{
    public class InventoryUpdatedMessage : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
